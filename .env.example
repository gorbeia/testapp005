# Optional: override the default Cloudflare Web Analytics beacon token
# (src/analytics.js) for this build, e.g. to point a fork at a different
# Cloudflare account. See docs/CLOUDFLARE_ANALYTICS.md.
VITE_CF_BEACON_TOKEN=
